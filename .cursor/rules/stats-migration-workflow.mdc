---
description: Миграция статистики внешних обработок и отчетов (пайплайн, размещение хелперов, soft-fail). Подключать при работе со статистикой / следующими объектами из списка.
alwaysApply: false
---

# Миграция статистики — handoff

Обращение: **Архитектор**. Список: `Куда добавлена статистика.txt` в корне Link.

## Пайплайн батча (3 объекта)

1. **3 редактора** (субагенты) — по одному объекту
2. **R1 — механика** (не размещение): имена хелперов, thin API, guards `Неопределено`, нет wide `Попытка`, soft-тексты без `.`/`!`, open/close сессии, `ТекстОшибки` при soft-fail+уведомлении, покрытие soft-fail (см. ниже)
3. **R2 — размещение / perf / читаемость** (не чеклист R1): где вызывается статистика → хелперы только там; запрет мертвого прокси форма→Object; `РеквизитФормыВЗначение` не на Дополнить/Окончание
4. **Приемка родителем** + правки

## Субагенты / API (жестко)

- Субагентов (`Task`) **можно** запускать.
- **Запрещено** передавать параметр `model` — только та же модель, что у родителя (подписка).
- **Запрещено** выбирать чужие/API-модели (Claude/GPT/Gemini и т.п.) — из-за этого сгорели платные токены.
- Если непонятно — не запускать `Task`, делать самому.

## Soft-fail (R1 п. покрытие)

Кандидат на `ДополнитьПараметрыВыполненияСессии` при открытой сессии: **реальная ошибка** (не инфо/успех), которая:
- срывает сценарий команды (`Возврат` / `Отказ`), **или**
- ошибка по элементу цикла + `Продолжить`

Не протягивать `ПараметрыСессии` «на всякий случай». Не третий этап ревью.

При soft-fail + сообщение пользователю — один текст через переменную:

```
ТекстОшибки = "...";
ДополнитьПараметрыВыполненияСессии(ПараметрыСессии, ТекстОшибки);
ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстОшибки);
```

## Размещение хелперов

- **Form-only** → хелперы только на форме, вызов регистра напрямую. ObjectModule: только bump версии.
- **Dual** (регламент Object + форма) → thin helpers в Object; на форме Дополнить/Окончание → регистр напрямую; старт — один RFVZ / `ЗарегистрироватьНовуюСессиюОбъекта`.
- **Object-only** (сессия в модуле объекта, форма не регистрирует) → хелперы только в Object; `Дополнить` только если есть вызов.
- Для отчетов форма стартует через `РеквизитФормыВЗначение("Отчет")`.
- Имена только полные: `ДополнитьПараметрыВыполненияСессии` / `ЗарегистрироватьНовуюСессию` / `ЗарегистрироватьОкончаниеСессии`. Запрещен короткий `Дополнить`.
- Старт: `ЗарегистрироватьНовуюСессиюОбъекта`; Окончание thin `(Механизм, Сессия)`; Дополнить → `Структура("ТекстОшибки", ...)`.
- **`ДополнитьПараметрыВыполненияСессии` объявлять только если в этом же модуле есть хотя бы один вызов.** Нет soft-fail в модуле → хелпера нет (не «для симметрии API»).
- Не удалять бизнес-процедуры. Хирургический diff. Без «е» вместо «ё» в новых текстах (не вводить «ё»).
- **Версия:** за один проход миграции объекта последний сегмент растет **ровно на 1** (не +2/+3 из-за правок по ревью — правки без повторного bump).

## Прогресс

Обработки из `Куда добавлена статистика.txt` мигрированы **полностью**.

Отчеты (батч из 3, список закрыт):
- `_ИмпортироватьСписокПротиводействияЛегализацииДоходовПоложениеЦБ639П` — form-only, 0.23→0.24
- `_ИнвентаризационнаяВедомость` — object-only (`Сформировать`), 0.08→0.09
- `_РасшифровкаАмортизацииДисконтаКорректировкиЭСП` — form-only, 0.06→0.07

Новых «следующих 3» нет.
