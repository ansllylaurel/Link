Ошибка при вызове метода контекста (Выполнить)
{ОбщийМодуль.РаботаСИсполнителями.Модуль(635)}:		РезультатЗапроса = Запрос.Выполнить();
{РегистрСведений.УсловияДоговоровОбщие.Форма.ФормаЗаписи.Форма(40)}:	АдресДереваДочернихЭлементов = РаботаСИсполнителями.ПолучитьДочерниеЭлементы(Запись.Договор, , Отбор, , ЭтаФорма.УникальныйИдентификатор);
по причине:
{(15, 69)}: Ожидается выражение ")"
И ДатаЗакрытия = ДАТАВРЕМЯ(1, 1, 1) ИЛИ ДатаЗакрытия > ДАТАВРЕМЯ(2 <<?>>026, 4, 18)

по причине:
{(15, 69)}: Ожидается выражение ")"
И ДатаЗакрытия = ДАТАВРЕМЯ(1, 1, 1) ИЛИ ДатаЗакрытия > ДАТАВРЕМЯ(2 <<?>>026, 4, 18)
