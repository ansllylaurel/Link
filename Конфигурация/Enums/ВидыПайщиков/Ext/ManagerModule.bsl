
Процедура ОбработкаПолученияДанныхВыбора(ДанныеВыбора, Параметры, СтандартнаяОбработка)
	
	Если Параметры.Свойство("ИсточникАнкетИЗаявок") Тогда
		
		Если Параметры.ИсточникАнкетИЗаявок = Перечисления.ИсточникиАнкетИЗаявок.Агент
			или Параметры.ИсточникАнкетИЗаявок = Перечисления.ИсточникиАнкетИЗаявок.Сайт Тогда
			Параметры.Отбор.Вставить("Ссылка", Владелец);
		КонецЕсли;
		
	КонецЕсли;		
		
КонецПроцедуры
