
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	//Если ПараметрКоманды.Количество() > 1 Тогда
	//	ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Проведение по бухгалтерскому учету для нескольких документов не реализовано. Будет проведен только документ: " + ПараметрКоманды[0]);
	//ИначеЕсли ПараметрКоманды.Количество() = 0 Тогда
	//	Возврат;
	//КонецЕсли; 
	
	Если ПараметрКоманды.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	//ПараметрыФормы = Новый Структура("Документ", ПараметрКоманды[0]);
	ПараметрыФормы = Новый Структура("Документы", ПараметрКоманды);
	ОткрытьФормуМодально("ОбщаяФорма.ФормаПроведенияПоБухгалтерскомуУчету", ПараметрыФормы, ПараметрыВыполненияКоманды.Источник, ПараметрыВыполненияКоманды.НавигационнаяСсылка);
	
КонецПроцедуры
