
Процедура ПередЗаписью(Отказ, Замещение)
	
	ДатаОбновления	= ТекущаяДата();
	
	Для каждого Запись Из ЭтотОбъект Цикл
		Если НЕ ЗначениеЗаполнено(Запись.ДатаОбновления) Тогда
			Запись.ДатаОбновления 	= ДатаОбновления;
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры
