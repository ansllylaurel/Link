
#Область События_формы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ЗаполнитьЗначенияСвойств(ЭтаФорма, Параметры,, "ЗакрыватьПриВыборе, ЗакрыватьПриЗакрытииВладельца, ТолькоПросмотр");
	
	ЭтаФорма.НалоговыйПериод = Параметры.ПоказательПериода;
	
	НастроитьПоНалоговомуПериоду(ЭтаФорма, Истина);
	
КонецПроцедуры

#КонецОбласти

#Область Команды_формы

&НаКлиенте
Процедура КомандаОК(Команда)
		
	Результат = Новый Структура;
		
	ДобавитьСвойствоВСтруктуруРезультата(Результат, "ПоказательПериода", 	НалоговыйПериод(
																				ЭтаФорма.НалоговыйПериод_Дата,
																				ЭтаФорма.НалоговыйПериод_Периодичность,
																				ЭтаФорма.НалоговыйПериод_Год,
																				ЭтаФорма.НалоговыйПериод_НомерПериода));
		
	ДобавитьСвойствоВСтруктуруРезультата(Результат, "ПоказательДаты",		ЭтаФорма.ПоказательДаты, 		"0");
	ДобавитьСвойствоВСтруктуруРезультата(Результат, "ПоказательНомера",		ЭтаФорма.ПоказательНомера, 		"0");
	ДобавитьСвойствоВСтруктуруРезультата(Результат, "ПоказательОснования",	ЭтаФорма.ПоказательОснования, 	"0");
	ДобавитьСвойствоВСтруктуруРезультата(Результат, "КодБК", 				ЭтаФорма.КодБК);
	ДобавитьСвойствоВСтруктуруРезультата(Результат, "КодОКТМО",				ЭтаФорма.КодОКТМО);
	ДобавитьСвойствоВСтруктуруРезультата(Результат, "ИдентификаторПлатежа", ЭтаФорма.ИдентификаторПлатежа);
	ДобавитьСвойствоВСтруктуруРезультата(Результат, "СтатусСоставителя", 	ЭтаФорма.СтатусСоставителя);
	
	ЭтаФорма.Закрыть(Результат);
	
КонецПроцедуры

&НаКлиенте
Процедура КомандаОтмена(Команда)

	Модифицированность = Ложь;
	ЭтаФорма.Закрыть(Неопределено);
	
КонецПроцедуры

#КонецОбласти

#Область События_элементов_формы

&НаКлиенте
Процедура НалоговыйПериод_ПериодичностьПриИзменении(Элемент)
	
	ИзменитьПериодичностьПеречисленияВБюджет(ЭтаФорма);	
	
КонецПроцедуры

#КонецОбласти

#Область Прочие_процедуры_и_функции

&НаСервереБезКонтекста
Функция ВидыНалоговыхПериодов()
	
	// Первые два знака налогового периода предназначены для определения периодичности уплаты
	
	Периоды = Новый СписокЗначений;
	Периоды.Добавить(ПериодичностьМесяц(),     "МС - месячный платеж");
	Периоды.Добавить(ПериодичностьКвартал(),   "КВ - квартальный платеж");
	Периоды.Добавить(ПериодичностьПолугодие(), "ПЛ - полугодовой платеж");
	Периоды.Добавить(ПериодичностьГод(),       "ГД - годовой платеж"); 
	Периоды.Добавить(ПлатежПоКонкретнойДате(), "Платеж по конкретной дате");
	Периоды.Добавить(НезаполненноеЗначение(),  "0 - значение не указывается");
	Периоды.Добавить(ИныеСлучаи(),             "00 - иные случаи");
	
	Возврат Периоды;
	
КонецФункции

&НаСервереБезКонтекста
Функция МаксимальноеЗначениеПоПериодичности(Периодичность)
	
	Результат = Неопределено;
	Если Периодичность = ПериодичностьМесяц() Тогда
		Результат = 12;
	ИначеЕсли Периодичность = ПериодичностьКвартал() Тогда
		Результат = 4;
	ИначеЕсли Периодичность = ПериодичностьПолугодие() Тогда
		Результат = 2;
	ИначеЕсли Периодичность = ПериодичностьГод() Тогда
		Результат = 1;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция НезаполненноеЗначение()
	
	// При невозможности указать конкретное значение, указывается ноль "0".
	// Наличие незаполненных реквизитов не допускается.
	
	Возврат "0";
	
КонецФункции

&НаСервереБезКонтекста
Функция ИныеСлучаи()
	
	Возврат "00";
	
КонецФункции

&НаСервереБезКонтекста
Функция ПериодичностьГод()
	
	Возврат "ГД";
	
КонецФункции

&НаСервереБезКонтекста
Функция ПериодичностьПолугодие()
	
	Возврат "ПЛ";
	
КонецФункции

&НаСервереБезКонтекста
Функция ПериодичностьКвартал()
	
	Возврат "КВ";
	
КонецФункции

&НаСервереБезКонтекста
Функция ПериодичностьМесяц()
	
	Возврат "МС";
	
КонецФункции

&НаСервереБезКонтекста
Функция ПлатежПоКонкретнойДате() 
	
	Возврат "-";
	
КонецФункции

&НаСервереБезКонтекста
Функция РеквизитЗаполнен(Значение) 
	
	Возврат ЗначениеЗаполнено(Значение) И СокрП(Значение) <> НезаполненноеЗначение();
	
КонецФункции

&НаСервереБезКонтекста
Процедура ПривестиЭлементыНалоговогоПериода(Периодичность, Год, НомерПериода, Дата)
	
	Если НЕ РеквизитЗаполнен(Периодичность)
		ИЛИ ВидыНалоговыхПериодов().НайтиПоЗначению(Периодичность) = Неопределено Тогда
		Периодичность = НезаполненноеЗначение();
		ГодПлатежа    = 0;
		НомерПериода  = 0;
		Дата          = '0001-01-01';
		Возврат;
	КонецЕсли;
	
	Если Периодичность = ПлатежПоКонкретнойДате() Тогда
		
		Если НЕ ЗначениеЗаполнено(Дата) Тогда
			Дата = ТекущаяДатаСеанса();
		КонецЕсли;
		
		ГодПлатежа = Год(Дата);
		
		Возврат;
		
	КонецЕсли;
	
	Если Год = 0 Тогда
		Год = Год(ТекущаяДатаСеанса());
	КонецЕсли;
	
	Если Периодичность = ПериодичностьГод() Тогда
		НомерПериода = 0;
		Дата = Дата(Год, 1, 1);
		Возврат;
	КонецЕсли;
	
	НомерПериода = Макс(1, НомерПериода);
	МаксимальноеЗначениеПоПериодичности = МаксимальноеЗначениеПоПериодичности(Периодичность);
	Если МаксимальноеЗначениеПоПериодичности = Неопределено Тогда
		Возврат;
	Иначе
		НомерПериода = Мин(НомерПериода, МаксимальноеЗначениеПоПериодичности);
	КонецЕсли;
	
	Дата = ДатаНачалаНалоговогоПериода(Периодичность, Год, НомерПериода);
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ДатаНачалаНалоговогоПериода(Периодичность, Год, НомерПериода)
	
	Если Год = 0 Тогда
		Возврат Дата(1, 1, 1);
	ИначеЕсли НомерПериода = 0 Тогда
		Возврат Дата(Год, 1, 1);
	ИначеЕсли Периодичность = ПериодичностьМесяц() Тогда
		Возврат Дата(Год, НомерПериода, 1);
	ИначеЕсли Периодичность = ПериодичностьКвартал() Тогда
		Возврат Дата(Год, НомерПериода * 3 - 2, 1);
	ИначеЕсли Периодичность = ПериодичностьПолугодие() Тогда
		Возврат Дата(Год, НомерПериода * 6 - 5, 1);
	Иначе
		Возврат Дата(Год, 1, 1);
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПреобразоватьДатуКСтроке(Дата)
	
	Если ТипЗнч(Дата) = Тип("Дата") И ЗначениеЗаполнено(Дата) Тогда
		Возврат Формат(Дата, "ДФ=dd.MM.yyyy");
	ИначеЕсли Дата = ИныеСлучаи() Тогда
		Возврат ИныеСлучаи();
	Иначе
		Возврат НезаполненноеЗначение();
	КонецЕсли;
	
КонецФункции

&НаСервереБезКонтекста
Функция НалоговыйПериод(Период, Знач Периодичность, Знач Год = Неопределено, Знач НомерПериода = Неопределено)
	
	Если Периодичность = НезаполненноеЗначение()
		ИЛИ ВидыНалоговыхПериодов().НайтиПоЗначению(Периодичность) = Неопределено Тогда
		
		Возврат НезаполненноеЗначение();
		
	ИначеЕсли Периодичность = ПлатежПоКонкретнойДате() Тогда
		
		Возврат ПреобразоватьДатуКСтроке(Период);
		
	КонецЕсли;
	
	// Для остальных нужен Год и НомерПериода
	Если (НомерПериода = Неопределено ИЛИ Год = Неопределено)
		И НЕ ЗначениеЗаполнено(Период) Тогда
		Возврат НезаполненноеЗначение();
	КонецЕсли;
	
	Если Год = Неопределено Тогда
		Год          = Год(Период);
	КонецЕсли;
	
	Если НомерПериода = Неопределено Тогда
		
		НомерПериода = 0;
		
		Если Периодичность = ПериодичностьКвартал() Тогда
			НомерПериода = (2 + Месяц(НачалоКвартала(Период))) / 3;
		ИначеЕсли Периодичность = ПериодичностьМесяц() Тогда
			НомерПериода = Месяц(Период);
		ИначеЕсли Периодичность = ПериодичностьПолугодие() Тогда
			Если Месяц(Период) <= 6 Тогда
				НомерПериода = 1;
			Иначе
				НомерПериода = 2;
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Периодичность
		+ "." + Формат(НомерПериода, "ЧЦ=2; ЧН=; ЧВН=")
		+ "." + Формат(Год, "ЧЦ=4; ЧГ=");
	
КонецФункции
	
&НаСервереБезКонтекста
Функция РазобратьНалоговыйПериод(Знач НалоговыйПериод)
	
	ОписаниеПериода = Новый Структура;
	ОписаниеПериода.Вставить("Периодичность", НезаполненноеЗначение());
	ОписаниеПериода.Вставить("Год",           0);
	ОписаниеПериода.Вставить("НомерПериода",  0);
	ОписаниеПериода.Вставить("Дата",          '0001-01-01');
		
	Если СтрДлина(НалоговыйПериод) <> 10 Тогда
		Возврат ОписаниеПериода;
	КонецЕсли;
	
	ЧастиПериода = РаботаСоСтрокамиКлиентСервер.РазложитьСтрокуВМассивПодстрок(НалоговыйПериод, ".", Ложь);
	Если ЧастиПериода.Количество() <> 3 Тогда
		Возврат ОписаниеПериода;
	КонецЕсли;
	
	// Два варианта:
	// 1.Первые два знака налогового периода предназначены для определения периодичности уплаты
	// 2.Форматом "ДД.ММ.ГГГГ" указывается конкретная дата
	
	Если СтрДлина(ЧастиПериода[0]) <> 2 Тогда
		Возврат ОписаниеПериода;
	КонецЕсли;
	
	Если НЕ РаботаСоСтрокамиКлиентСервер.ТолькоЦифрыВСтроке(ЧастиПериода[1])
		ИЛИ НЕ РаботаСоСтрокамиКлиентСервер.ТолькоЦифрыВСтроке(ЧастиПериода[2]) Тогда
		Возврат ОписаниеПериода;
	КонецЕсли;
	
	Периодичность = ЧастиПериода[0];
	Если РаботаСоСтрокамиКлиентСервер.ТолькоЦифрыВСтроке(Периодичность) Тогда
		Периодичность = ПлатежПоКонкретнойДате();
	ИначеЕсли ВидыНалоговыхПериодов().НайтиПоЗначению(Периодичность) = Неопределено Тогда
		Возврат ОписаниеПериода;
	КонецЕсли;
	
	Если Периодичность = ПлатежПоКонкретнойДате() Тогда
		
		Попытка
			Значение = Дата(ЧастиПериода[2], ЧастиПериода[1], ЧастиПериода[0]);
		Исключение
			Значение = Неопределено;
		КонецПопытки;
		
		Если Значение = Неопределено Тогда
			Возврат ОписаниеПериода;
		Иначе
			ОписаниеПериода.Дата = Значение;
			ОписаниеПериода.Год  = Год(Значение);
		КонецЕсли;
		
	Иначе
		
		Попытка
			НомерПериода = Число(ЧастиПериода[1]);
			Год          = Число(ЧастиПериода[2]);
		Исключение
			НомерПериода = 0;
			Год          = 0;
		КонецПопытки;
		
		Если Год = 0 Тогда
			Возврат ОписаниеПериода;
		Иначе
			
			МаксимальноеЗначениеПоПериодичности = МаксимальноеЗначениеПоПериодичности(Периодичность);
			Если МаксимальноеЗначениеПоПериодичности = Неопределено ИЛИ НомерПериода > МаксимальноеЗначениеПоПериодичности Тогда
				Возврат ОписаниеПериода;
			КонецЕсли;
			
			ОписаниеПериода.НомерПериода = НомерПериода;
			ОписаниеПериода.Год          = Год;
			ОписаниеПериода.Дата         = ДатаНачалаНалоговогоПериода(Периодичность, Год, НомерПериода);
			
		КонецЕсли;
		
	КонецЕсли;
	
	ОписаниеПериода.Периодичность = Периодичность;
	
	Возврат ОписаниеПериода;
	
КонецФункции

&НаКлиенте
Процедура ДобавитьСвойствоВСтруктуруРезультата(Результат, ИмяСвойства, ЗначениеСвойства, ЗначениеПоУмолчанию = Неопределено)
		
	Если Не ПустаяСтрока(ЗначениеСвойства) Тогда
		Результат.Вставить(ИмяСвойства, СокрЛП(ЗначениеСвойства));
	ИначеЕсли ЗначениеПоУмолчанию <> Неопределено Тогда	
		Результат.Вставить(ИмяСвойства, ЗначениеПоУмолчанию);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Процедура ИзменитьПериодичностьПеречисленияВБюджет(Форма)
	
	ПривестиЭлементыНалоговогоПериода(
		Форма.НалоговыйПериод_Периодичность,
		Форма.НалоговыйПериод_Год,
		Форма.НалоговыйПериод_НомерПериода,
		Форма.НалоговыйПериод_Дата);
	
	НастроитьПоНалоговомуПериоду(Форма, Ложь);
	
КонецПроцедуры
	
&НаКлиентеНаСервереБезКонтекста
Процедура НастроитьПоНалоговомуПериоду(Форма, ЗаполнитьВспомогательныеПоля = Истина)
	
	Элементы = Форма.Элементы;
	
	Если ЗаполнитьВспомогательныеПоля Тогда		
		ОписаниеПериода = РазобратьНалоговыйПериод(Форма.НалоговыйПериод);
		Форма.НалоговыйПериод_Периодичность = ОписаниеПериода.Периодичность;
		Форма.НалоговыйПериод_Год           = ОписаниеПериода.Год;
		Форма.НалоговыйПериод_НомерПериода  = ОписаниеПериода.НомерПериода;
		Форма.НалоговыйПериод_Дата          = ОписаниеПериода.Дата;
	КонецЕсли;
	
	Периодичность = Форма.НалоговыйПериод_Периодичность;
	
	Элементы.ГодПлатежа.Видимость             = Ложь;
	Элементы.ПериодПлатежаПолугодие.Видимость = Ложь;
	Элементы.ПериодПлатежаКвартал.Видимость   = Ложь;
	Элементы.ПериодПлатежаМесяц.Видимость     = Ложь;
	Элементы.ДатаПлатежа.Видимость            = Ложь;
	
	Если Периодичность = ПлатежПоКонкретнойДате() Тогда
		Элементы.ДатаПлатежа.Видимость = Истина;
	ИначеЕсли Периодичность = ПериодичностьГод() Тогда
		Элементы.ГодПлатежа.Видимость = Истина;
	ИначеЕсли Периодичность = ПериодичностьМесяц() Тогда
		Элементы.ГодПлатежа.Видимость         = Истина;
		Элементы.ПериодПлатежаМесяц.Видимость = Истина;
	ИначеЕсли Периодичность = ПериодичностьКвартал() Тогда
		Элементы.ГодПлатежа.Видимость           = Истина;
		Элементы.ПериодПлатежаКвартал.Видимость = Истина;
	ИначеЕсли Периодичность = ПериодичностьПолугодие() Тогда
		Элементы.ГодПлатежа.Видимость             = Истина;
		Элементы.ПериодПлатежаПолугодие.Видимость = Истина;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
