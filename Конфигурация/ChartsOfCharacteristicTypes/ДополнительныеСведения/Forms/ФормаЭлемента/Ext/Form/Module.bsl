
&НаСервере
Процедура ПриЧтенииСозданииНаСервере()
	
	Элементы.ОбъектыНазначения.Видимость = Не Объект.НазначатьВсемОбъектам;
	
КонецПроцедуры // ПриЧтенииСозданииНаСервере


&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ПриЧтенииСозданииНаСервере();
	
КонецПроцедуры

&НаСервере
Процедура ПриЧтенииНаСервере(ТекущийОбъект)
	
	ПриЧтенииСозданииНаСервере();
	
КонецПроцедуры

&НаКлиенте
Процедура НазначатьВсемОбъектамПриИзменении(Элемент)
	
	Если Не Объект.НазначатьВсемОбъектам И Объект.ОбъектыНазначения.Количество() > 0 Тогда
		Если Вопрос("Список объектов назначения будет очищен. Продолжить?", РежимДиалогаВопрос.ДаНет) = КодВозвратаДиалога.Нет Тогда
			Объект.НазначатьВсемОбъектам = Не Объект.НазначатьВсемОбъектам;
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	Элементы.ОбъектыНазначения.Видимость = Не Объект.НазначатьВсемОбъектам;
	
КонецПроцедуры
