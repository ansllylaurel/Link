# Link — правила разработки 1С

Рабочая директория ГК РЕГИОН / 1С УК. Все подпроекты (`Конфигурация`, `Обработки`, и т.д.) пишутся по одним стандартам.

## Источники правил

1. **Корпоративные** — папка `Стандарты/` (Confluence «Стандарты разработки в 1С:Предприятие 8»). При конфликте они главнее.
2. **Фирмы «1С»** — [Система стандартов ITS (v8std)](https://its.1c.ru/db/v8std); удобное зеркало: https://v8std.ru/
3. Правила Cursor — `.cursor/rules/*.mdc` (подключаются автоматически).

## Что агент обязан соблюдать

- Русский BSL, CamelCase, области `#Область`.
- Хардкод ссылок только через `ОбщегоНазначения.ПолучитьИменованныйОбъект(...)`.
- Запросы: `ВЫБРАТЬ РАЗРЕШЕННЫЕ`, временные таблицы с `_`, МВТ вместо индексов `ВыполнитьПакет`, текст открывается в конструкторе.
- Методы `&НаСервере` с постфиксом `НаСервере`; минимум серверных вызовов.
- Транзакции по паттерну #std783; без пустых `Исключение`.
- Интерфейсные строки через `НСтр`.

Подробности — в `.cursor/rules/`.
