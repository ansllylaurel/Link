
Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)
	
	УниверсальныеМеханизмы.ЗаполнитьОбщиеРеквизитыСправочника(ЭтотОбъект, ДанныеЗаполнения);
	
КонецПроцедуры

Процедура ПриКопировании(ОбъектКопирования)
	
	УниверсальныеМеханизмы.ЗаполнитьОбщиеРеквизитыСправочника(ЭтотОбъект);
	
КонецПроцедуры

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	
	УниверсальныеМеханизмы.ПослеОбработкиПроверкиЗаполнения(ЭтотОбъект, Отказ);

КонецПроцедуры

Процедура ПередЗаписью(Отказ)
	
	// Блокирующие проверки
	
	Если Не УниверсальныеМеханизмы.ОбъектМожноЗаписать(ЭтотОбъект, Отказ) Тогда
		Возврат;
	КонецЕсли;
	
	// Если указана дата начала периодичности начисления задолженности, то требуется проверить,
	// что после нее во всех документах этого вида задолженности заполнен период начисления
	
	Если ЗначениеЗаполнено(ЭтотОбъект.ДатаНачалаПериодичностиНачисления) И Не ЭтотОбъект.ЭтоНовый() И Не ЭтотОбъект.ПометкаУдаления Тогда
			
		Если Не Документы.НачислениеЗадолженности.ПроверитьЗаполнениеПериодаНачисленияЗадолженности(ЭтотОбъект.Ссылка, ЭтотОбъект.ДатаНачалаПериодичностиНачисления) Тогда	
			
			ТекстОшибки = СтрШаблон("После %1 найдены начисления задолженности %2 с незаполненным периодом начисления", Формат(ЭтотОбъект.ДатаНачалаПериодичностиНачисления, "ДФ=dd.MM.yyyy"), НРег(ЭтотОбъект));
			
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстОшибки, ЭтотОбъект, "ДатаНачалаПериодичностиНачисления", , Отказ);
			
		КонецЕсли;		
		
	КонецЕсли;
		
КонецПроцедуры
