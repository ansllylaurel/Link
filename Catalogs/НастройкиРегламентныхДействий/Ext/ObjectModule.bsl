
Процедура ПередЗаписью(Отказ)
	
	Если НЕ ЭтоНовый() Тогда
		
		МассивСсылок = Новый Массив;
		МассивСсылок.Добавить(ЭтотОбъект.Ссылка);
		
		Если НайтиПоСсылкам(МассивСсылок).Количество() <> 0 Тогда
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Эта настройка используется. Невозможно ее изменить!",,,,Отказ);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры
