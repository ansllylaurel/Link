Проверка после исправлений (2026-06-04)
========================================

ИСПРАВЛЕНО:
- Двойной префикс ВыполнитьВыполнить (4 файла): _ИмпортДанныхДляОтчетности, _ОповещенияОСредствах..., _ОповещениеПриПервом..., _ПометкаНеиспользуемых...
- 82 обёртки команд: многострочный Попытка → хелперы Выполнить* (29 модулей, см. fix_try_log.txt)
- Двойная статистика _СверкаСБрокеромВБРР: ОбработатьОтчетыБрокера → ВыполнитьОбработатьОтчетыБрокера
- Восстановлены повреждённые модули (скрипт вставлял хелперы внутрь #Область):
  _УниверсальныйОбменДаннымиXML (УправляемаяФорма)
  _РуДата
  _ФормированиеРегистрацийСведенийОБумагахНаНовуюОферту
  _СборкаРелиза

АВТОПРОВЕРКА (баланс Процедура/КонецПроцедуры, мусорные строки, многострочный Попытка в обёртках): 0 замечаний

ОСТАЁТСЯ (не синтаксические ошибки 1С):
- _6НДФЛ_2025: особый паттерн сбора статистики (ПараметрыСессииСбораСтатистики, без имени команды в сессии)
- _РасчетСценарногоАнализа (12 форм): хелперы ВыполнитьКоманда*() без ПараметрыСессии — мягкие Возврат не пишут ТекстОшибки
- Отдельные команды без хелпера (1–2 строки в Попытка): СохранитьПараметры, ВыгрузкаОчиститьУзлыОбмена и др. — допустимо по правилу «1 строка», но при расширении нужен хелпер
- _ЗагрузкаСтоимостьСПФИ (корень и Конфигурация): тело Сформировать в многострочном Попытка — см. эталон _ЗаполнениеОтветственного

Рекомендация: открыть конфигурацию в 1С и выполнить синтаксический контроль модулей из fix_try_log.txt
